"use client";

import { Header } from "@/components";
import { navData } from "@/lib/data";

const PaymentMethod = () => (
  <>
    <Header navData={navData.paymentMethod} />
  </>
);

export default PaymentMethod;
