"use client";

import { Header } from "@/components";
import { navData } from "@/lib/data";

const Notification = () => (
  <>
    <Header navData={navData.notification} />
  </>
);

export default Notification;
